# OpenCXD: An Open Real-Device-Guided Hybrid Evaluation Framework for CXL-SSDs

## Project Directory
```
root
├── macsim (git submodule)
│   (components related to x86 system simulation part of OpenCXD, including benchmark configuration files from SkyByte)
└── openssd
    (components for running OpenSSD code on DaisyPlus platform (Write Log, Log Index, Data Cache, etc))
```